//*textContent, will ignore HTML tags and display only text

let menu = document.getElementById('menu')


console.log(menu.textContent) //will ignore CSS rules
console.log(menu.innerText) //only visible text, follow CSS rules

 